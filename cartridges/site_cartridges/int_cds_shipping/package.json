{
    "name": "int_cds_shipping",
    "version": "1.0.1",
    "description": "CDS Shipping site cartridge — registers sfcc.app.shipping.estimate/quote/calculate hooks (mock data).",
    "hooks": "./cartridge/scripts/hooks.json",
    "caches": "./caches.json",
    "scripts": {
        "test": "mocha --recursive test/unit/**/*.test.js",
        "test:coverage": "nyc mocha --recursive test/unit/**/*.test.js"
    },
    "devDependencies": {
        "mocha": "^10.7.3",
        "chai": "^4.5.0",
        "sinon": "^15.2.0",
        "proxyquire": "^2.1.3",
        "nyc": "^15.1.0"
    }
}
